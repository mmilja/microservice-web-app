apiVersion: v1
kind: Service
metadata:
  name: {{ template "web-app-chart.fullname" . }}
  labels:
    app: {{ template "web-app-chart.name" . }}
    chart: {{ template "web-app-chart.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "web-app-chart.name" . }}
    release: {{ .Release.Name }}
